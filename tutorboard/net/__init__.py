"""The network this board is reachable on, and the one it teaches over.

`tailscale` is how one address survives moving between compute nodes; `egress`
is whether a turn can reach its model at all, which is a different question and
a different network path.
"""
