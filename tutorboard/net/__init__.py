"""Reaching the other machine.

Two machines teach from one address, and neither can read the other's disk. Every
question one asks the other goes through here: who is up (`tailscale`), how to
get a connection at all where there is no interface (`socks`), what is answering
on a port (`boards`), and whether a turn can reach its model (`egress`).
"""
